<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SportclubApp.Maui.ViewModels"
             x:Class="SportclubApp.Maui.Views.LoginPage"
             x:DataType="vm:LoginViewModel"
             Title="{Binding Title}"
             Shell.NavBarIsVisible="False">

    <Grid Padding="{StaticResource PagePadding}" RowDefinitions="*,Auto,*">
        <VerticalStackLayout Grid.Row="1" Spacing="{StaticResource SpacingM}">
            <Label Text="Sportclub"
                   Style="{StaticResource HeroTitle}" />
            <Label Text="Sign in to manage your classes"
                   Style="{StaticResource Caption}"
                   HorizontalOptions="Center"
                   Margin="{StaticResource MarginBottomM}" />

            <Entry Placeholder="Email"
                   Keyboard="Email"
                   Text="{Binding Email}" />
            <Entry Placeholder="Password"
                   IsPassword="True"
                   Text="{Binding Password}" />

            <Button Text="Sign in"
                    Style="{StaticResource AccentButton}"
                    Command="{Binding SignInCommand}"
                    IsEnabled="{Binding IsNotBusy}" />

            <ActivityIndicator IsRunning="{Binding IsBusy}"
                               IsVisible="{Binding IsBusy}"
                               HorizontalOptions="Center" />

            <Label Text="{Binding ErrorMessage}"
                   Style="{StaticResource ErrorText}"
                   IsVisible="{Binding HasError}"
                   HorizontalOptions="Center" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>
